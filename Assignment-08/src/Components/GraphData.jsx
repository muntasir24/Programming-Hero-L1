import React from 'react';

const GraphData = ({ myapp }) => {
const {description, ratings,} = myapp || {};

    return (
        <div>
            
        </div>
    );
};

export default GraphData;